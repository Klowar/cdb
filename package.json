{
  "name": "cdb",
  "version": "0.0.1",
  "description": "Column db on node",
  "main": "-",
  "author": "Klowar",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rm -rf build",
    "build": "tsc",
    "start": "node build/index.js",
    "test": "mocha --require ts-node/register 'test/**/*.ts'",
    "compile": "node_modules/.bin/jison compile/sql.jison compile/sql.jisonlex"
  },
  "devDependencies": {
    "@types/mocha": "^8.2.0",
    "@types/node": "^14.14.16",
    "jison": "^0.4.18",
    "mocha": "^8.2.1",
    "ts-node": "^9.0.0",
    "typescript": "^4.1.2"
  }
}
