
// 128 Mb
export const DEFAULT_LOCK_SIZE = 1024 * 1024 * 128;
// 2 seconds
export const DEFAULT_FLUSH_TICK = 2 * 1000;
// Forced flush after
export const DEFAULT_FORCED_FLUSH_AFTER = 2 * DEFAULT_FLUSH_TICK;
// Max one flush size
export const MAX_FLUSH_SIZE = 128;
// Flush call waterlinee
export const FLUSH_CALL_WATERLINE = 128;
